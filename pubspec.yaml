name: accounting_app
description: 简约记账应用 - 支持多账本、分类管理、支付渠道、统计图表
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.17.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.0.0
  path: ^1.8.3
  provider: ^6.0.0
  fl_chart: ^0.55.1
  intl: ^0.17.0
  table_calendar: ^3.0.0
  shared_preferences: ^2.0.0
  uuid: ^3.0.0

dev_dependencies: {}

flutter:
  uses-material-design: true
