name: flutter_personal_accounting
description: 个人记账应用 - 简单易用的收支管理工具
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.17.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # 使用更兼容的依赖版本
  sqflite: ^2.0.0+4
  path_provider: ^2.0.0
  intl: ^0.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
